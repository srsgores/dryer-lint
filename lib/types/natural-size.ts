/** What the natural size rules are allowed to let through. */

/** The one exception to natural sizing, which is a cap on how wide text is allowed to get. */
export interface NaturalSizeOptions {
	allowMaxInline?: boolean;
}
