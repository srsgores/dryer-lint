/** The shapes the stylelint plugins work with, which postcss describes loosely. */

/** What a project can say about how strictly it wants sizes read. */
export interface NaturalSizeSecondary {
	allowMaxInline?: boolean;
}
